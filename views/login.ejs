<!DOCTYPE html>
<html>
<head>
  <title>Admin Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex justify-center items-center h-screen">
  <form action="/admin/login" method="POST" class="bg-white p-6 rounded shadow-md w-full max-w-sm">
    <h2 class="text-2xl font-bold mb-4 text-center">Admin Login</h2>
    <% if (error) { %>
      <p class="text-red-500 mb-4"><%= error %></p>
    <% } %>
    <input name="username" placeholder="Username" required class="mb-3 w-full px-4 py-2 border rounded">
    <input name="password" type="password" placeholder="Password" required class="mb-4 w-full px-4 py-2 border rounded">
    <button class="bg-green-600 text-white px-4 py-2 rounded w-full hover:bg-green-700">Login</button>
  </form>
</body>
</html>
