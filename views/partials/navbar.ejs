<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Report Portal</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50">

  <!-- Professional Navbar -->
  <nav class="bg-white shadow-md border-b border-gray-200">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between h-16 items-center">
        <a href="/" class="text-2xl font-bold text-gray-800 flex items-center gap-2 hover:text-purple-700 transition">
          📘 <span>Report Portal</span>
        </a>
        <div class="flex items-center space-x-4 text-sm sm:text-base">
          <a href="/admin/reports" class="text-purple-600 font-semibold hover:text-purple-800 transition">Reports</a>
          <% if (!isAdmin) { %>
            <a href="/admin/login" class="text-green-600 font-semibold hover:text-green-700 transition">Login</a>
          <% } else { %>
            <a href="/admin/report-upload" class="text-blue-600 font-semibold hover:text-blue-700 transition">Upload</a>
            <a href="/admin/logout" class="text-red-600 font-semibold hover:text-red-700 transition">Logout</a>
          <% } %>
        </div>
      </div>
    </div>
  </nav>

</body>
</html>
